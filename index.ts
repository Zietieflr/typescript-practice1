// not always the best practice, but good to practice with
const isAwesome: boolean = true;

function sum(number1: number, number2: number): number {
  return number1 + number2
}

sum(1, 2)
sum(1, "Fred")